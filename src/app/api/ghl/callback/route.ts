import { NextRequest, NextResponse } from "next/server"; export async function GET(req: NextRequest) { const code = req.nextUrl.searchParams.get("code"); const error = req.nextUrl.searchParams.get("error"); console.log("GHL OAuth callback:", { code, error }); const message = error ? ("OAuth error: " + error) : "Success! You can close this tab. (We received the code and will finish the setup next.)"; return new NextResponse(message, { status: 200 }); }
